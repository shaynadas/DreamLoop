numpy>=1.24
pandas>=2.0
pyarrow>=14.0
pillow>=10.0
opencv-python>=4.8
tqdm>=4.66
scipy>=1.11

# Waymo Open Dataset parsing (TFRecord + proto)
# Pin tensorflow to a version that matches the Waymo SDK build.
# On Apple Silicon, use tensorflow-macos instead of tensorflow.
waymo-open-dataset-tf-2-12-0; platform_machine != "arm64"
tensorflow==2.12.0; platform_machine != "arm64"
# Apple Silicon path: install the waymo SDK from source, or do Waymo parsing on GX10 only.
tensorflow-macos==2.13.0; sys_platform == "darwin" and platform_machine == "arm64"

# Perception
ultralytics>=8.1
torch>=2.1
torchvision>=0.16

# Dashboard (Team AI uses this; lives here so both sides share venv)
streamlit>=1.30
plotly>=5.18
