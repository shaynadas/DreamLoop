# DreamLoop core pipeline — Linux x86_64 AND aarch64 (GX10 / Grace Blackwell).
# Python 3.10 recommended; 3.11 also works for this core set.
#
# This file does NOT include TensorFlow or the Waymo SDK. The Waymo SDK has
# no aarch64 wheel and TF 2.12 is locked to x86_64 + Python ≤3.11. If you
# need to decode .tfrecord on THIS box (you probably don't — see below),
# install requirements-waymo.txt separately and accept that it will fail
# on aarch64.
#
# Recommended Waymo workflow on the GX10:
#   1. Decode .tfrecord on a Linux x86_64 box (laptop, cloud VM, Docker).
#   2. scp the resulting waymo_intermediate/<segment>/ directory to the GX10.
#   3. Run the rest of the pipeline (perturb → rds_writer → cosmos → eval) here.
#   The intermediate dir is small (parquets + PNGs), maybe 100–300 MB.

# Core scientific stack.
numpy>=1.23
pandas>=2.0
pyarrow>=14.0
pillow>=10.0
scipy>=1.11
tqdm>=4.66

# OpenCV — headless: no X11 deps, smaller install, same API we use.
opencv-python-headless>=4.8

# ── PERCEPTION (yolo_eval.py) ──
# torch should be CUDA-enabled. The Cosmos venv on this box already has a
# CUDA torch — if you're sharing that venv, skip torch here. Otherwise:
#
#   pip install torch torchvision --index-url \
#       https://download.pytorch.org/whl/cu124
#
# …THEN pip install -r requirements.txt for the rest.
torch>=2.1
torchvision>=0.16
ultralytics>=8.1

# ── DASHBOARD ──
streamlit>=1.30
plotly>=5.18
