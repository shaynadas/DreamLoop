# DreamLoop pipeline — Linux x86_64 (GX10).
#
# ───────────────────────────────────────────────────────────────────────
#  REQUIRED Python: 3.10
#
#  The Waymo SDK (waymo-open-dataset-tf-2-12-0) only ships wheels for
#  CPython 3.8 – 3.10. TF 2.12 caps out at Python 3.11. Don't fight it:
#
#      sudo apt install python3.10 python3.10-venv          # Ubuntu 22.04
#      python3.10 -m venv .venv
#      source .venv/bin/activate
#      pip install --upgrade pip
#      pip install -r requirements.txt
#
#  If you're on Python 3.11+ and don't need to decode Waymo .tfrecords on
#  this box, comment out the two "WAYMO" lines and skip waymo_loader.py.
# ───────────────────────────────────────────────────────────────────────

# Core scientific stack. numpy pinned <1.24 for TF 2.12 compatibility;
# the upper bound matters more than the lower.
numpy>=1.23,<1.24
pandas>=2.0,<2.2
pyarrow>=14.0
pillow>=10.0
scipy>=1.11
tqdm>=4.66

# OpenCV — headless wheel: no GUI deps, smaller install, same VideoWriter/
# VideoCapture/imwrite API we use. If you ALSO need cv2.imshow() (you don't
# on a server), swap to opencv-python.
opencv-python-headless>=4.8

# ── WAYMO (waymo_loader.py) ──
# These two MUST stay together. The SDK wheel is built against TF 2.12.0
# exactly. Picking a different TF version breaks the proto bindings.
tensorflow==2.12.0
waymo-open-dataset-tf-2-12-0

# ── PERCEPTION (yolo_eval.py) ──
# torch wheel selection: by default pip pulls CPU wheels on Linux. If you
# want CUDA (you do, on the GX10), install torch separately FIRST:
#
#   pip install torch==2.4.* torchvision --index-url \
#       https://download.pytorch.org/whl/cu124
#
# …then `pip install -r requirements.txt` for the rest. The version
# constraints below are loose so the CUDA install above isn't overwritten.
torch>=2.1
torchvision>=0.16
ultralytics>=8.1

# ── DASHBOARD (Streamlit pivot) ──
streamlit>=1.30
plotly>=5.18
